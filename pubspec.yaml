name: cats
description: "A project about cats"
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  http: ^1.4.0
  intl: any

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.26.2

flutter:
  uses-material-design: true
